{
  "name": "your-project",
  "scripts": {
    "test": "jest",
    "testreport": "jest --reporters=default --reporters=jest-html-reporter",
    "testtxt":"jest > test_report.txt 2>&1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-graphql": "^0.12.0",
    "graphql": "^16.8.1",
    "graphql-http": "^1.22.1",
    "supertest": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-html-reporter": "^3.10.2"
  }
}
